api_key: ${LIVEKIT_API_KEY}
api_secret: ${LIVEKIT_API_SECRET}
ws_url: ws://127.0.0.1:7880
redis:
  address: 127.0.0.1:6379
log_level: info
# Track egress only (per-leg audio files); room composites would need Chromium + more RAM.
