# TLS via Let's Encrypt. api.<PUBLIC_HOST> / lk.<PUBLIC_HOST> must be DNS-only A records (Cloudflare proxy off) so ACME and WebSockets hit this box directly.
api.${PUBLIC_HOST} {
	reverse_proxy 127.0.0.1:8000
}

lk.${PUBLIC_HOST} {
	reverse_proxy 127.0.0.1:7880
}
